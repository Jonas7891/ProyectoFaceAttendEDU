databaseChangeLog:
  - changeSet:
      id: audit-001-create-audit-schema
      author: Andres Gutierrez
      changes:
        - sql:
            sql: CREATE SCHEMA IF NOT EXISTS audit;
      rollback:
        - sql:
            sql: DROP SCHEMA IF EXISTS audit CASCADE;