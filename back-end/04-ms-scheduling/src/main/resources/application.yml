server:
  port: 8087
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/faceattend_db
    username: postgres
    password: postgres
    hikari:
      schema: scheduling
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate.default_schema: scheduling
