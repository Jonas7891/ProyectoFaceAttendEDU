package com.faceattend_edu.newModule.domain.port;

import com.faceattend_edu.newModule.domain.model.School;
import com.faceattend_edu.util.domain.AbstractRepositoryPort;

import java.util.UUID;

public interface SchoolRepositoryPort extends AbstractRepositoryPort<School, UUID> {
}
