package com.faceattend_edu.audit.domain.port;

import com.faceattend_edu.audit.domain.model.Log;
import com.faceattend_edu.util.domain.AbstractRepositoryPort;

public interface LogRepositoryPort extends AbstractRepositoryPort<Log, Long> {
}
